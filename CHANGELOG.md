# 📋 سجل التغييرات — MaghzAI NotebookLM

جميع التغييرات الملحوظة على هذا المشروع ستُوثَّق في هذا الملف.

الصيغة مبنية على [Keep a Changelog](https://keepachangelog.com/ar/1.1.0/)،
والمشروع يلتزم بـ [التخزين الدلالي](https://semver.org/lang/ar/).

---

## [غير مُصدر] — قيد التطوير

### مُضاف
- توثيق شامل بالعربية في سجل التغييرات وسجل التحديثات.

### مُصحَّح
- إصلاح فشل البناء الإنتاجي (`next build`) بسبب فحص `JWT_SECRET` في وقت استيراد الوحدة.
  - السبب: `src/lib/auth.ts` و `src/middleware.ts` كانا يستدعيان `resolveJwtSecret()` على مستوى الوحدة، فكان استيراد أي وحدة مصادقة أثناء البناء (مثل `/api/auth/logout`) يرمي خطأً في بيئة البناء لأن `NODE_ENV=production` ولا يوجد `JWT_SECRET` (المفتاح مطلوب فقط في وقت تشغيل الطلبات).
  - الحل: تحويل التحقق إلى استدعاء كسول (Lazy) مُخزَّن مؤقتاً عبر `getJwtSecret()` لا يُنفَّذ إلا عند توقيع/تحقق الرمز فعلياً، مع بقاء سلوك الأمان نفسه (رمي الخطأ عند غياب/ضعف المفتاح في الإنتاج).
  - النتيجة: نجح البناء بدون `JWT_SECRET` (Compiled successfully، 20/20 صفحة ثابتة) وكذلك معه، و`tsc --noEmit` بدون أخطاء.
- إصلاح خطأ تحليل `tailwindcss` أثناء البناء (`Can't resolve 'tailwindcss'`).
  - السبب: ذاكرة تخزين مؤقتة قديمة (`.next`) تحمل مساراً خاطئاً لمجلد المشروع.
  - الحل: مسح ذاكرة التخزين المؤقتة `.next` و `node_modules/.cache` وإعادة البناء من جديد.
  - النتيجة: اكتمل البناء بنجاح (Compiled successfully in 4.4min، TypeScript في 112s).

---

## [v0.1.0] — 2026-07-01

### 🎉 الإصدار الأول

النسخة الافتتاحية لمنصة **بحّاثة — MaghzAI NotebookLM**، المستوحاة من Google NotebookLM والمصممة خصيصاً للمحتوى العربي والتعليمي.

### مُضاف

#### إدارة الدفاتر البحثية
- إنشاء وتحرير وحذف دفاتر بحثية متعددة.
- توليد تلقائي للعنوان والوصف والرمز التعبيري (Emoji) للمصادر.
- واجهة شبكية لعرض جميع الدفاتر مع عدد المصادر.

#### إدارة المصادر (5 طرق)
- **نص** — لصق نص مباشر كمرجع.
- **رابط** — استخراج المحتوى من صفحات الويب والمقالات بواسطة Cheerio.
- **يوتيوب** — استخراج النص التلقائي (Transcript) مع بيانات الفيديو بواسطة yt-caption-kit.
- **ملف** — رفع ملفات PDF و TXT و MD (حتى 20 ميجابايت) بواسطة pdf-parse.
- **بحث عميق** — بحث في الإنترنت وجمع معلومات شاملة (سريع/عميق).

#### المحادثة التعليمية الذكية
- إجابات تعليمية شاملة مدعومة بـ Google Gemini AI.
- استشهادات مرجعية لكل إجابة مرتبطة بالمصادر.
- اقتراحات أسئلة المتابعة (توسيع، ارتباط، أمثلة، تعمق).
- توسيع الإجابة بمعلومات إضافية من الويب.
- توليد أسئلة مقترحة تلقائياً من المصادر.
- نسخ وحفظ الإجابات كملاحظات.
- وضع احتياطي للتحليل النصي المحلي عند عدم توفر مفتاح Gemini.

#### استوديو التعلم (7 أنواع)
- ملخص شامل منظم بعناوين فرعية ونقاط.
- أسئلة شائعة (8-12 سؤالاً مع إجابات مفصلة).
- دليل دراسي (أهداف، مفاهيم، مصطلحات، نقاط مراجعة).
- جدول زمني للتسلسل المنطقي/الزمني للأحداث.
- خريطة ذهنية مرئية باستخدام Mermaid.
- بطاقات تعليمية (10-15 بطاقة للمراجعة والحفظ).
- عرض تقديمي (8-12 شريحة مع ملاحظات للمحاضر).

#### ميزات إضافية
- الوضع الداكن/الفاتح مع حفظ التفضيل ودعم تفضيل النظام.
- تصميم متجاوب بالكامل (جوال/كمبيوتر) مع نظام تبويبات للشاشات الصغيرة.
- واجهة عربية RTL أصيلة مع خط Cairo.
- لوحات قابلة للطي لتخصيص مساحة العمل.
- عرض المصادر مع المحتوى الكامل.
- تحرير الملاحظات يدوياً.
- مشغل النظرة الصوتية (Audio Overview) التفاعلي.
- دعم المعادلات الرياضية (KaTeX) و Markdown (react-markdown).
- بحث نصي كامل باستخدام PostgreSQL (`to_tsvector` / `to_tsquery`) بدون APIs خارجية.

#### واجهة برمجة التطبيقات (API)
- `/api/health` — فحص صحة التطبيق.
- `/api/auth/login`، `/api/auth/logout`، `/api/auth/me`، `/api/auth/register` — المصادقة.
- `/api/notebooks` — CRUD للدفاتر البحثية.
- `/api/notebooks/[id]/chat` و `/api/notebooks/[id]/chat/expand` — المحادثة والتوسعة.
- `/api/notebooks/[id]/notes` — إدارة الملاحظات.
- `/api/notebooks/[id]/sources` — إدارة المصادر ورفع الملفات والبحث العميق.
- `/api/notebooks/[id]/studio` — توليد المواد التعليمية.
- `/api/notebooks/[id]/suggestions` — اقتراحات الأسئلة.

### 🔧 تقنيات
- Next.js 16.2.6 (App Router) مع React 19.2.6 و TypeScript 5.9.3.
- Tailwind CSS 4.1.17 مع `@tailwindcss/postcss` و `@tailwindcss/typography`.
- PostgreSQL مع Drizzle ORM 0.45.2 و drizzle-kit 0.31.10.
- Google Gemini AI مع نماذج flash-lite / flash / pro القابلة للتكوين.
- Mermaid 11.16.0 للخرائط الذهنية و KaTeX 0.18.1 للمعادلات.
- Lucide React 1.28.0 للأيقونات.

---

## صيغة السجل

- `مُضاف` — لميزات جديدة.
- `مُغيَّر` — للتغييرات على الوظائف القائمة.
- `مُصحَّح` — لإصلاحات الأخطاء.
- `مُزال` — لميزات تمت إزالتها.
- `مُحدَّث` — للتحسينات والتحديثات.

---

<div align="center">

**صُنع بـ ❤️ للمحتوى العربي والتعليم الذكي**

</div>